base_config: configs/base.yaml

manifest_path: data/manifests/painting_progress.jsonl
experiment_dir: experiments/painting
modality: image

encoder:
  embedding_dim: 64
  bands: 4

model:
  hidden_dim: 256
  dropout: 0.1

training:
  curriculum_level: 2
  batch_size: 8
  epochs: 8
  learning_rate: 0.0005
  device: cpu
