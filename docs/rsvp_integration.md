# RSVP integration

Each draft can be assigned scalar density, vector flow, and entropy diagnostics.

\[
r_t = (\Phi_t, v_t, S_t)
\]

These diagnostics can be concatenated with latent states for field-aware trajectory learning.
